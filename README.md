# generic-terraform-module-template

This is to be used as a template to create new HCP Terraform private registry module repositories. Please update the `_summary.yaml` and `CODEOWNERS` files to reflect accurate owners.

This repository is not released to the HCP Terraform private registry as a module.
